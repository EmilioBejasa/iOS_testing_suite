included:
  - Sources
  - Tests
  - QuoteBox
  - QuoteBoxWidget
  - QuoteBoxTests
  - QuoteBoxUITests
excluded:
  - QuoteBox.xcodeproj

# SwiftLint's default rule set, enforced via --strict in CI. A first
# report-only run found 35 pre-existing violations (all cosmetic); those were
# fixed rather than silenced with `disabled_rules`.
