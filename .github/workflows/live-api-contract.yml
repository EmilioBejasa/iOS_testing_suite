name: Live Open-Meteo Contract Check

on:
  schedule:
    - cron: "0 13 * * *"
  workflow_dispatch:

jobs:
  contract:
    uses: ./.github/workflows/reusable-live-contract.yml
    with:
      scheme: Weather
      project: Weather.xcodeproj
      only_testing: "WeatherTests/OpenMeteoLiveContractTests"
